[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "parkinson-eeg"
version = "0.1.0"
description = "Reproducible resting-state EEG analyses for Parkinson disease"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]

